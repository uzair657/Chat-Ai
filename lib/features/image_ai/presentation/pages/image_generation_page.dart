import 'package:flutter/material.dart';

class ImageGenerationPage extends StatelessWidget {
  const ImageGenerationPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Images Generation Page"),
      ),
    );
  }
}
