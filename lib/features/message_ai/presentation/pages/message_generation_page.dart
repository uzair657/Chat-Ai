import 'package:flutter/material.dart';

class MessageGenerationPage extends StatelessWidget {
  const MessageGenerationPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Messages Generation Page"),
      ),
    );
  }
}